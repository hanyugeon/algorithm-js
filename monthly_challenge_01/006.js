// 삼각 달팽이(좌표 찍기)
// 나의 풀이
function solution(n) {
  const answer = [];
  let snail = [];

  for (let i = 1; i < n + 1; i++) {
    const temp = Array(i).fill(0);
    snail.push(temp);
  }

  



  return ;
}

/**
 * fill()
 * map()
 * flatMap()
 * concat()
 */

/**
 * 좌표 찍기.
 * 좌표를 어떠한 기준으로 옮겨갈것인가?
 * 좌표를 옮긴다음 해당 숫자 넣기
 * 모든 숫자가 조건에 맞게 들어간 이후에 하나의 배열로 반환.
 */